# Prototype — asset-kcht-dashboard

Design gate: **prototype + reviewUrl** — `autoApprove=ON` → agent confirm.

- **reviewUrl:** `file:///D:/AI-QLBD/Linm.RMMS.Data/specs/asset-kcht-dashboard/ui/prototype/asset-kcht-dashboard-prototype.html`
- Pack kind: `dashboard` (Kind E tiles)
- Variants: Standalone (Zone A + grid) · Widget body only (no H1)

Pict tile = GIS `assetIconBareHtml(iconCode)` (ESM import Demo). **Cấm** FA trên ô tài sản. `file://` có thể CORS → serve HTTP hoặc fallback mã trên vòng icon.

Put HTML prototype here; `ui/design.md` cites browser-openable **reviewUrl**.
