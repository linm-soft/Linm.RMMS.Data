# QA bugs — asset · task_4ec34586

## GAP-QA-STORE-03 (Must)
- Maestro Android assert `#row-asset-0` FAIL on `#sc-asset-list`.
- Evidence: `qa/screens/_maestro_android` · P6-CORE EmptyChrome.

## GAP-MOB-ASSET-AND-FETCH-01 (Must)
- Emulator mở list nhưng **0** `GET .../asset/road-assets` trong Mobile.Bff log (`10.0.2.2`).
- Host/iOS `GET road-assets` → 200 + items; Android vẫn EmptyChrome «Chưa có tài sản».
- Hypothesis: `AssetListScreen` `LaunchedEffect(onBack, onOpenDetail)` restart/cancel trước `Appear` → không `load()`.
- Fix hint (Dev): `LaunchedEffect(Unit) { Appear }` + set handlers tách `DisposableEffect`.

## GAP-MOB-UX-DUAL-01 (Must)
- iOS A3-CORE: live rows · Android P6-CORE: empty — dual not aligned.

## Non-block notes
- `auth/profile` Android → BFF rewrite log `web-bff` 500 (login vẫn 200) — theo dõi riêng.
- Live-only EmptyChrome đúng khi BE empty — **không** áp dụng ở đây (BE có data).
