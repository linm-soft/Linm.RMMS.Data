# Catalog quyền RMMS

> SSOT: `Linm.RMMS.Permission.Job/README.md` + `catalog/` — **không** import Auth `data/packages`.

Xem: `Linm.RMMS.Permission.Job/catalog/REVIEW-AUTH.md`

Job runtime chỉ Active/Inactive/gia hạn. Package `RMMS-*` + codes `kind=new` chỉ ở Job.
