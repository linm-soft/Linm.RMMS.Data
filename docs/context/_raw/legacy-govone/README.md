# Legacy GOVOne capture output

Generated by `tools/legacy-govone-capture` (local or CI `legacy-govone-capture.yml`).

| Path | Content |
|------|---------|
| `CAPTURE-SUMMARY.md` | Run summary |
| `FEATURE-MAP.md` | Slug → pages |
| `features/{slug}.md` | Field/UI extract per QLBD feature |
| `pages/*.json` | Raw page inventory (local/CI artifact) |
| `screenshots/` | PNG (gitignored) |

**Credentials:** never stored here. Use `.env.local` or GitHub secrets `GOVONE_USER` / `GOVONE_PASS`.

Downstream: `/docs-scan-cache` → `/qlbd-analy-demo` · `/qlbd-legacy-capture`.
