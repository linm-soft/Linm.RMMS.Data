# Sự cố — form org-tree

**Page:** `IncidentFormPage` · **route:** `/su-co` · **testIdPrefix:** `rmms-incident`  
**km_fields:** `km_skip` — giữ `KmStart`/`KmEnd` sẵn có, không đổi.  
**Parent:** [`reports-form-org.md`](reports-form-org.md)

Create + Edit: `RmmsOrgFormFields` · required Tuyến (`routeCode`).  
Người xử lý = `assigneeName` (person) · không thay bằng Đơn vị.
